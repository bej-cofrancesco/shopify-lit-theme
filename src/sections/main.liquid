{% schema %}
{
  "name": "t:sections.main.name",
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "t:sections.main.settings.collection.label",
      "info": "t:sections.main.settings.collection.info"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 2,
      "max": 12,
      "step": 1,
      "default": 4,
      "label": "t:sections.main.settings.products_to_show.label"
    }
  ]
}
{% endschema %}
