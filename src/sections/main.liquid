<section class="container mx-auto space-y-4 px-4 py-12">
  <h1 class="text-3xl font-semibold tracking-tight">{{ shop.name }}</h1>
  <p class="max-w-xl text-stone-600">
    Empty starter — add Lit islands under
    <code class="text-sm">src/frontend/components</code>
    and wrap them with
    <code class="text-sm">{% render 'vulpine-loader' %}</code>.
  </p>
</section>

{% schema %}
{
  "name": "t:sections.main.name",
  "settings": []
}
{% endschema %}
