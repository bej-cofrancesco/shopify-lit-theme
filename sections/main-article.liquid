<article>
  <h1 class="text-3xl font-semibold">{{ article.title }}</h1>
  <div class="prose mt-6">{{ article.content }}</div>
</article>

{% schema %}
{
  "name": "t:sections.main-article.name",
  "settings": []
}
{% endschema %}
